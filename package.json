{
  "name": "clm-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "cat styles/base.css styles/layout.css styles/components.css styles/animations.css | csso --output styles/main.min.css",
    "build:js": "terser js/main.js --compress --mangle --output js/main.min.js",
    "build": "npm run build:css && npm run build:js"
  },
  "devDependencies": {
    "csso-cli": "^4.0.2",
    "terser": "^5.31.0"
  }
}
